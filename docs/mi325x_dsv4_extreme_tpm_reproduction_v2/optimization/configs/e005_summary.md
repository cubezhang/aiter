# E005 service probe

Router port 18080 was unreachable before candidate execution. No performance claim or model change resulted. The host-wide FMoE filename search was discarded as noisy; subsequent discovery is restricted to the task-owned serving container.
